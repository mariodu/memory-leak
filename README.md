# Memory Leak Demo

## Leak

`npm start`

See the output in terminal.

## unLeak

remove comment before line 41
`// noRelease.splice(index, 1);`

then `npm start`

## draw image

`npm run plot`

## clean

`npm run clean`
